# Optional (build-time) key for local dev / deployments that rebuild the app.
# For a public open-source deploy, prefer entering your key in the app UI (stored in localStorage).
REACT_APP_POLLINATIONS_API_KEY=PASTE_YOUR_KEY_HERE
